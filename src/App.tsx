import { useState } from "react";
import reactLogo from "./assets/react.svg";
import viteLogo from "/vite.svg";
import "./App.css";



function App() {
  const [count, setCount] = useState(0);

  // task name (required, min 5, max 30)
  // priority (required, Urgent, High, Normal, Low)
  // story points (required, 1-20)
  // assignee (required, required, letters and spaces)
  // required (required, valid date in the future)


  return (
    <>

    </>
  );
}

export default App;
